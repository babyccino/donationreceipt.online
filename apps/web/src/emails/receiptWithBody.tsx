import { WithBody } from "components/dist/receipt/email"
import { previewEmailProps } from "@/emails/props"

const PreviewWithBody = () => <WithBody {...previewEmailProps} />
export default PreviewWithBody
