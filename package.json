{
  "name": "donation-receipt-online",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "npm@9.6.3",
  "scripts": {
    "dev": "npm exec turbo dev",
    "build": "npm exec turbo build",
    "test:unit": "npm exec turbo test:unit",
    "patch": "patch-package",
    "patch:generate": "patch-package jose"
  },
  "devDependencies": {
    "patch-package": "^8.0.0",
    "turbo": "^1.12.5-canary.0",
    "turborepo": "^0.0.1"
  }
}
